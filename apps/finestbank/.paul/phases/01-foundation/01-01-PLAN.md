---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.js
  - tailwind.config.js
  - tsconfig.json
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - backend/main.py
  - backend/requirements.txt
  - backend/app/__init__.py
  - backend/app/core/config.py
  - docker-compose.yml
  - .env.example
  - .gitignore
autonomous: true
---

<objective>
## Goal
Scaffold the full FinestBank monorepo: Next.js 14 frontend + FastAPI backend + Docker Compose for PostgreSQL/TimescaleDB/Redis — with both servers running and health-checked.

## Purpose
Everything else depends on this. Clerk auth, DB schema, real-time — all need the scaffold to exist first. This plan creates the skeleton; subsequent plans fill it.

## Output
- `frontend/` — Next.js 14 + Tailwind CSS + shadcn/ui (dark mode, base layout)
- `backend/` — FastAPI app with health endpoint and CORS configured
- `docker-compose.yml` — PostgreSQL + TimescaleDB + Redis services
- `.env.example` — all required env vars documented
- Both servers start cleanly; Docker services come up healthy
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Stack Reference
- Frontend: Next.js 14 (App Router), Tailwind CSS, shadcn/ui, TypeScript
- Backend: FastAPI (Python 3.11+), uvicorn
- DB: postgres:16-alpine + timescale/timescaledb extension
- Cache/Pub-Sub: Redis 7
- Auth (next plan): Clerk
</context>

<acceptance_criteria>

## AC-1: Frontend Starts Clean
```gherkin
Given the monorepo is cloned and npm install has run in frontend/
When the developer runs `npm run dev` in frontend/
Then Next.js starts on localhost:3000 with no errors, showing the FinestBank placeholder page in dark mode
```

## AC-2: Backend Starts Clean
```gherkin
Given Python deps are installed in backend/
When the developer runs `uvicorn app.main:app --reload` in backend/
Then FastAPI starts on localhost:8000 and GET /health returns { "status": "ok" }
```

## AC-3: Docker Services Start Healthy
```gherkin
Given Docker Desktop is running
When the developer runs `docker compose up -d` from the repo root
Then PostgreSQL (port 5432), TimescaleDB extension enabled, and Redis (port 6379) all report healthy status
```

## AC-4: Environment Variables Documented
```gherkin
Given a new developer clones the repo
When they read .env.example
Then all required env vars are listed with descriptions and example values (DATABASE_URL, REDIS_URL, NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY, CLERK_SECRET_KEY, JWT_SECRET)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 14 frontend with Tailwind + shadcn/ui dark mode base</name>
  <files>
    frontend/package.json,
    frontend/next.config.js,
    frontend/tailwind.config.js,
    frontend/tsconfig.json,
    frontend/src/app/layout.tsx,
    frontend/src/app/page.tsx,
    frontend/src/app/globals.css,
    frontend/components.json,
    frontend/.env.local.example
  </files>
  <action>
    Create frontend/ directory with Next.js 14 (App Router, TypeScript).

    package.json dependencies:
    - next@14, react@18, react-dom@18
    - tailwindcss, @tailwindcss/forms, postcss, autoprefixer
    - @radix-ui/react-slot, class-variance-authority, clsx, tailwind-merge (shadcn/ui deps)
    - lucide-react (icons)
    - recharts (charts — used later)

    tailwind.config.js:
    - darkMode: "class"
    - extend colors: primary blue #3B82F6, accent emerald #10B981, and dark background scale (gray-950, gray-900, gray-800)
    - content paths include src/**

    globals.css:
    - @tailwind directives
    - :root and .dark CSS variables for shadcn/ui color tokens
    - Set html class="dark" as default (finance dashboard = dark mode always)
    - Background: #0a0f1e (deep navy, finance feel)

    layout.tsx:
    - html lang="en" className="dark"
    - body with bg-gray-950 text-white antialiased
    - Placeholder metadata: title "FinestBank", description "Financial Intelligence Dashboard"

    page.tsx:
    - Placeholder page: centered div with "FinestBank" heading (text-3xl font-bold text-blue-400) and "Financial Intelligence Dashboard" subtitle
    - Note: "Phase 1 scaffold — auth and dashboard coming next"

    components.json (shadcn/ui config):
    - style: "default", rsc: true, tsx: true, tailwind aliases set up

    Avoid: installing full shadcn CLI via npx (just set up the config manually — CLI runs interactively)
    Avoid: using Pages Router — App Router only
  </action>
  <verify>cd frontend && npm install && npm run dev — Next.js starts on localhost:3000, dark navy background visible, "FinestBank" heading rendered</verify>
  <done>AC-1 satisfied: Frontend starts on :3000 with dark mode base layout</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold FastAPI backend with health endpoint and CORS</name>
  <files>
    backend/requirements.txt,
    backend/app/__init__.py,
    backend/app/main.py,
    backend/app/core/__init__.py,
    backend/app/core/config.py,
    backend/app/api/__init__.py,
    backend/app/api/health.py,
    backend/.env.example
  </files>
  <action>
    Create backend/ directory with FastAPI project structure.

    requirements.txt:
    - fastapi==0.111.0
    - uvicorn[standard]==0.29.0
    - python-dotenv==1.0.1
    - pydantic-settings==2.2.1
    - httpx==0.27.0 (for testing)
    - psycopg2-binary==2.9.9 (DB driver — used next plan)
    - alembic==1.13.1 (migrations — used next plan)
    - redis==5.0.4 (pub/sub — used phase 4)
    - faker==24.11.0 (seed data — used plan 01-03)

    app/core/config.py:
    - Pydantic BaseSettings class
    - Fields: DATABASE_URL, REDIS_URL, SECRET_KEY, CLERK_SECRET_KEY, CORS_ORIGINS (list)
    - Loads from .env via python-dotenv

    app/main.py:
    - FastAPI app instance with title="FinestBank API", version="0.1.0"
    - CORSMiddleware: allow_origins from settings.CORS_ORIGINS, allow_credentials=True, allow_methods=["*"], allow_headers=["*"]
    - Include health router at prefix /api
    - Root route GET / returns {"message": "FinestBank API", "docs": "/docs"}

    app/api/health.py:
    - GET /health returns {"status": "ok", "version": "0.1.0"}

    backend/.env.example:
    - DATABASE_URL=postgresql://finestbank:secret@localhost:5432/finestbank
    - REDIS_URL=redis://localhost:6379
    - SECRET_KEY=change-me-in-production
    - CLERK_SECRET_KEY=sk_test_...
    - CORS_ORIGINS=["http://localhost:3000"]

    Avoid: putting secrets in committed files — only .env.example with placeholder values
  </action>
  <verify>cd backend && pip install -r requirements.txt && uvicorn app.main:app --reload — GET http://localhost:8000/health returns {"status": "ok", "version": "0.1.0"}</verify>
  <done>AC-2 satisfied: Backend starts on :8000 and /health responds correctly</done>
</task>

<task type="auto">
  <name>Task 3: Docker Compose for PostgreSQL + TimescaleDB + Redis</name>
  <files>
    docker-compose.yml,
    docker-compose.override.yml,
    .env.example,
    .gitignore
  </files>
  <action>
    Create docker-compose.yml in repo root:

    services:

    db:
      image: timescale/timescaledb:latest-pg16
      container_name: finestbank_db
      environment:
        POSTGRES_USER: finestbank
        POSTGRES_PASSWORD: secret
        POSTGRES_DB: finestbank
      ports: ["5432:5432"]
      volumes: [finestbank_db_data:/var/lib/postgresql/data]
      healthcheck:
        test: ["CMD-SHELL", "pg_isready -U finestbank"]
        interval: 10s
        timeout: 5s
        retries: 5

    redis:
      image: redis:7-alpine
      container_name: finestbank_redis
      ports: ["6379:6379"]
      healthcheck:
        test: ["CMD", "redis-cli", "ping"]
        interval: 10s
        timeout: 5s
        retries: 5

    volumes:
      finestbank_db_data:

    Root .env.example:
    - DATABASE_URL=postgresql://finestbank:secret@localhost:5432/finestbank
    - REDIS_URL=redis://localhost:6379
    - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
    - CLERK_SECRET_KEY=sk_test_...
    - JWT_SECRET=change-me-32-chars-minimum

    .gitignore (repo root):
    - .env, .env.local, **/.env
    - __pycache__/, *.pyc, .pytest_cache/
    - node_modules/, .next/, out/
    - *.db, *.sqlite
    - .DS_Store

    Avoid: committing any .env file with real credentials
    Avoid: using latest tag for postgres without pinning — use timescale/timescaledb:latest-pg16
  </action>
  <verify>docker compose up -d && docker compose ps — both db and redis show status "healthy"; psql postgresql://finestbank:secret@localhost:5432/finestbank -c "SELECT default_version FROM pg_available_extensions WHERE name = 'timescaledb';" returns a version row</verify>
  <done>AC-3 + AC-4 satisfied: Docker services healthy, all env vars documented in .env.example</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Nothing locked yet — this is the first plan

## SCOPE LIMITS
- No auth implementation in this plan (Clerk setup is 01-02)
- No database schema or migrations (that's 01-03)
- No real dashboard UI — placeholder page only
- No seed data — defer to 01-03
- Do not wire frontend → backend in this plan (just get both running independently)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run dev` — starts on :3000 without errors
- [ ] `cd backend && uvicorn app.main:app --reload` — starts on :8000 without errors
- [ ] `GET http://localhost:8000/health` returns `{"status": "ok", "version": "0.1.0"}`
- [ ] `GET http://localhost:8000/docs` — Swagger UI loads
- [ ] `docker compose up -d && docker compose ps` — db and redis both healthy
- [ ] `.env.example` exists in root and in backend/ with all 5 required vars
- [ ] No .env files committed (only .env.example)
- [ ] `localhost:3000` shows dark navy background with "FinestBank" heading
</verification>

<success_criteria>
- All 3 tasks completed without errors
- Both servers start from a clean clone with documented setup steps
- Docker services reach healthy state
- No secrets committed to git
- Dark mode base layout visible at localhost:3000
</success_criteria>

<output>
After completion, create `.paul/phases/01-foundation/01-01-SUMMARY.md`
</output>
